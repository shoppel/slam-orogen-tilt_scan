# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(tilt_scanTaskLib)
ADD_LIBRARY(${TILT_SCAN_TASKLIB_NAME} SHARED 
    ${TILT_SCAN_TASKLIB_SOURCES})

add_dependencies(${TILT_SCAN_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${TILT_SCAN_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TILT_SCAN_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${TILT_SCAN_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TILT_SCAN_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TILT_SCAN_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TILT_SCAN_TASKLIB_HEADERS}
    DESTINATION include/orocos/tilt_scan)

